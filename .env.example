# Eurogames Backend API Configuration
EUROGAMES_API_URL=https://eurogames.web-c10.workers.dev

# Authorization Bearer Token
EUROGAMES_API_KEY=your_bearer_token_here

# Simple Auth (optional - if not set, site is public)
# Shared password for accessing the site
AUTH_PASSWORD=your_password_here
# Secret key for signing session cookies (use a long random string)
AUTH_SECRET=generate_a_random_secret_here
